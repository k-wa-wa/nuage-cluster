# Managed by SealedSecrets. Replace with sealed values before applying.
# Required keys: dsn
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: pechka-postgres
  namespace: pechka
spec:
  encryptedData:
    dsn: REPLACE_WITH_SEALED_VALUE
  template:
    metadata:
      name: pechka-postgres
      namespace: pechka
    type: Opaque
