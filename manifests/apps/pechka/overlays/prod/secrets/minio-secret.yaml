# Managed by SealedSecrets. Replace with sealed values before applying.
# Required keys: MINIO_ROOT_USER, MINIO_ROOT_PASSWORD
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: minio-secret
  namespace: pechka
spec:
  encryptedData:
    MINIO_ROOT_USER: REPLACE_WITH_SEALED_VALUE
    MINIO_ROOT_PASSWORD: REPLACE_WITH_SEALED_VALUE
  template:
    metadata:
      name: minio-secret
      namespace: pechka
    type: Opaque
