# Managed by SealedSecrets. Replace with sealed values before applying.
# Required keys: MONGO_INITDB_ROOT_USERNAME, MONGO_INITDB_ROOT_PASSWORD
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: mongodb-secret
  namespace: pechka
spec:
  encryptedData:
    MONGO_INITDB_ROOT_USERNAME: REPLACE_WITH_SEALED_VALUE
    MONGO_INITDB_ROOT_PASSWORD: REPLACE_WITH_SEALED_VALUE
  template:
    metadata:
      name: mongodb-secret
      namespace: pechka
    type: Opaque
