# ServiceAccount のトークンは k8s 1.24 以降自動生成されないため、明示的に発行する。
apiVersion: v1
kind: Secret
metadata:
  name: autopilot-token
  annotations:
    kubernetes.io/service-account.name: autopilot
type: kubernetes.io/service-account-token
