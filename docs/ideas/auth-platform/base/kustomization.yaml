apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: auth-platform

helmCharts:
  - name: authentik
    repo: https://charts.goauthentik.io
    version: 2025.10.3
    releaseName: authentik
    namespace: authentik
    valuesFile: authentik-values.yaml
